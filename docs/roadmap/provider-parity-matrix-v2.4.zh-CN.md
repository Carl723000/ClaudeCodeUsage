# v2.4 Provider 交互一致性矩阵

简体中文 · [English](provider-parity-matrix-v2.4.md)

快照日期：2026-09-08；本地候选分支：`codex/v2.3.2-stabilization`。提交号只是开发
证据，不代表已发布版本的声明。

本矩阵记录交互一致性，但不假装 Claude 与 Codex 提供了等价数据。“已对齐”表示用户
交互与视觉层级一致；“刻意不同”表示现有来源证据无法支持同一行为；“部分完成”表示
仍有已知契约需要实现或验证；“本地候选”表示行为只存在于本地分支，并已通过表中所列
测试。

## 组件矩阵

| 区域 | Claude | Codex | 状态 | 证据与边界 |
|:--|:--|:--|:--|:--|
| Provider 导航 | 共用 tablist、自动激活和 roving 键盘焦点 | 相同 | 已对齐 | `providerNavClient.ts`；`tests/ui/codex-accessibility.spec.mjs` 覆盖键盘与 Axe |
| 仪表盘导航 | 今日 / 最近 30 天 / 全部时间，以及 Claude 可支持的目的地 | 共用同一组标签，但省略无法支持的分支和工作流 | 已对齐，目的地刻意不同 | `showTab` 统一状态与 ARIA；`codex-interactions.spec.mjs` 覆盖导航和重载 |
| 摘要卡 | Claude 成本、消息数、Token 与缓存总量 | 明确限定的 API 等价值和 Codex 原生 Token 总量 | 布局已对齐，指标刻意不同 | `codex-visual.spec.mjs` 验证共用生产样式与几何 |
| 模型折叠项 | 绿色显示成本，折叠后显示 Token 细节 | 绿色显示 API 等价值，折叠后显示 Codex 原生 Token 细节 | 已对齐 | `codex-visual.spec.mjs` 验证标题语义和共用折叠几何 |
| 今日小时主图 | 成本及 Token/消息指标切换 | API 等价值及 Codex Token/线程指标切换 | 已对齐 | 共用图表控件、网格、堆叠成本样式和配置时区小时标签 |
| 今日小时选择 | 可选小时、同步详情行与表格行 | 相同 | 本地候选（`fbaa693`） | `codex-interactions.spec.mjs` 覆盖鼠标、Enter、Space、指标同步、零主机消息、重载恢复和切换标签重置 |
| 今日 Token 构成 | Claude 输入、缓存写入、缓存读取、输出 | 未缓存输入、缓存输入、输出；推理继续作为输出子集说明，而不是第二次计入输出 | 已对齐，Token 语义刻意不同 | 共用构成图；Codex 不重复计算推理 |
| 最近 30 天主图 | 每日值与指标切换 | 采用 Codex 原生指标的相同交互 | 已对齐 | 共用图表栈和 `chart-date-labels.spec.mjs`；配置时区的日期键保持稳定 |
| 最近 30 天“日 → 小时” | 点击/表格折叠会要求主机重新聚合已加载到内存的 Claude 记录 | 点击/表格折叠直接使用 Webview 中持久化的滚动小时侧车 | 部分完成 | 两条路径点击时都不重读 JSONL；Codex 已物化，Claude 仍会执行 O(records) 内存重聚合，应改用已物化的近期小时映射 |
| 全部时间“月 → 日” | 点击/表格折叠按配置时区重新聚合已加载记录 | 不提供入口：Codex 索引目前有全部时间月汇总，但只有最近 30 天日汇总 | 刻意不同 | 不增加无响应的 Codex 控件；只有在有界存储下存在经审计的全部时间日聚合后才补齐 |
| 图表展开语义 | 同时只展开一个周期，表格按钮与图表状态同步，并使用 `aria-expanded` / `aria-controls` | 在可支持的“日 → 小时”中相同 | 已对齐 | `codex-interactions.spec.mjs` 和 `codex-accessibility.spec.mjs` 覆盖重载与键盘 |
| 空数据/部分小时详情 | 在受控详情行中显示 Claude 无数据响应 | 显式空数据行，并显示小时迁移覆盖情况 | 信息层级已对齐，来源说明刻意不同 | Codex 不回退到未经验证的旧总量 |
| 缺失周期补零 | 今日完整展示 24 小时；最近 30 天精确展示配置时区内连续 30 个自然日，同时不扩大来源聚合 | 今日覆盖完整时展示全部 24 小时；索引未完整时仍保持稀疏；最近 30 天精确展示 30 天 | 本地候选（`7b86a54`） | 合成的零值行显示为真实 0，而不是“未定价”；Claude 零值日期不显示无响应的下钻控件。辅助函数单测与 `time-range-presentation.spec.mjs` 覆盖精确范围 |
| 指标持久化 | 所选指标在完整 Webview 重载后保留 | 相同 | 已对齐 | `chartMetrics` 状态及浏览器测试 |
| 展开/选择持久化 | 展开项和今日小时选择在 Webview 重载后保留，用户主动切换顶层标签时重置 | 在受支持交互中相同 | 重载已对齐，实时刷新部分完成 | 实时内容替换还要证明焦点和滚动锚点保持 |
| 页面滚动 | 按 provider/标签防抖保存位置 | 相同 | 重载已对齐；重复写保护为本地候选（`7b86a54`） | 连续滚动只在手势结束后写一次；Chromium 延迟触发的 scroll/pagehide 事件不会同步重复写入相同位置；`codex-interactions.spec.mjs` 覆盖重载恢复 |
| 表格 | 共用数值对齐、可排序语义和有界横向滚动容器 | 相同 | 已对齐 | 覆盖键盘排序、360 px、最长语言和桌面溢出 |
| 每周额度等价值详情 | Provider 专属 Claude 面板，详情默认折叠 | 独立的 Provider 专属 Codex 面板，详情默认折叠 | 布局已对齐，证据权威性刻意不同 | Claude 使用官方观测；Codex 使用本地末次观测；Compare 从不相加两者 |
| 状态栏额度详情 | 官方 `/usage`，共用进度表和阈值 | 本地末次观测，使用同一进度表和阈值 | 布局已对齐，权威性刻意不同 | Provider 来源说明必须保留，不能因颜色相同而暗示证据等价 |
| 分享 | v2.3.x 暂时保留旧 Claude 导出兼容性 | Compare 工作台导出隐私安全的 provider 分量 | 按决策部分完成 | 新分享能力只进入 Compare 工作台；旧入口需等文档和兼容迁移后再移除 |
| 设置 | 简洁、按 provider 生效的控件 | 相同 | 已对齐 | 详细本地数据清单与破坏性清理控制只保留在 repo 文档 |
| 图表可访问替代 | 交互控件已有名称与状态，空状态为 live region | 相同 | 部分完成 | 每张完整图仍需 provider 专属文本替代或带标签区域；Compare 中外观相同的区域也要有唯一可访问名称 |
| 已安装 VSIX 证据 | 本分支尚未截取 | 本分支尚未截取 | 部分完成 | 候选打包必须在真实 VS Code 中截取状态栏、额度 tooltip 和两级下钻的隐私安全图片 |

## 读取与持久化边界

| 交互 | 激活时允许读取的来源 | 跨 Webview 重载保留 | 重置边界 |
|:--|:--|:--|:--|
| 今日小时选择 | 无；只操作 DOM 状态 | 所选小时与所选指标 | 用户切换顶层仪表盘标签 |
| 最近 30 天“日 → 小时”（Claude） | 只使用已存在的内存记录；不读取文件系统 | 展开日期与指标 | 用户切换顶层仪表盘标签 |
| 最近 30 天“日 → 小时”（Codex） | 只使用已物化的 Webview DTO；不发送主机消息 | 展开日期与指标 | 用户切换顶层仪表盘标签 |
| 全部时间“月 → 日”（Claude） | 只使用已存在的内存记录；不读取文件系统 | 展开月份与指标 | 用户切换顶层仪表盘标签 |
| 排序/筛选 | 只使用已渲染 DTO | 排序、会话范围和模型筛选 | 用户显式修改或重置 UI |
| 滚动 | 只记录浏览器滚动位置 | 按 provider 和仪表盘标签保存 | 重置 UI |

本表中的任何交互都不得触发 JSONL 重扫。新增聚合必须在既有索引/更新流程中构建，并先
通过有界 DTO 传入 Webview，才能增加新的下钻控件。

## 下一步实现顺序

1. 将 Claude 的滚动小时物化结果传给 Webview，使“日 → 小时”不再在激活时重聚合全部
   已加载记录。
2. 在实时数据替换后保留选择、展开链、键盘焦点与最近滚动锚点，而不仅是完整 Webview
   重载。
3. 为图表补齐带 provider 名称的区域标签或文本替代，并验证 Compare 中外观相似区域的
   可访问名称唯一。
4. 评估全部时间 Codex 日聚合的存储与迁移成本。在可接受之前，“月 → 日”保持为显式
   语义差异，不加入无功能控件。

# V2.3.0 Codex Beta R5 发布交接单（中文版）

更新日期：2026-07-22

## 结论

V2.3.0 的方案 B 已完成到本地安装验收阶段。Codex 不再是嵌入式的简化面板，
而是与现有 Claude 仪表板共用同一套视觉、导航、状态和设置语言的三页产品：
**概览（Overview）**、**探索（Explore）**、**优化建议（Recommendations）**。
设置是从当前主页面进入并返回的辅助页面，不是第四个主标签页。

本轮没有把 Claude 的数据模型硬套给 Codex：不估算 Codex 美元成本，不检查、分析或
保留对话正文字段，也不虚构无法从本地日志可靠获得的 Branches／Workflows。Codex 的额度只
标为日志中的“最后观测值”，不冒充实时订阅余额。

R5 另外修复了真实环境中“已启用 Codex Beta、重载后仍完全没有结果”的阻塞问题：
持久索引在一次写入后出现合法 JSON 末尾附加数据，旧版会在加载阶段永久报错，甚至无法
进入本地日志扫描。现在只对 JSON 损坏或不支持的 schema 执行原子隔离，完整保留
`.corrupt-*.json` 备份并从空索引重建；权限等其他 I/O 错误仍然失败关闭。

## 版本与成品

- 开发分支：`codex/v2.3.0-codex-beta`
- V2.2.1 发布后基线：`2be7f52`
- 实现与质量门禁 HEAD：`c0be71a6e69385093ba1f8ba157b626126fdb06d`
- 相对基线：91 个实现 commit、128 个文件，新增 31,783 行、删除 1,029 行。
- 本地测试包：
  `/Users/carl/Jiaming/ClaudeCodeUsage-MyFix/claude-code-usage-2.3.0-codex-beta-r5.vsix`
- VSIX 内部版本：`GrowthJack.claude-code-usage@2.3.0`
- VSIX 文件数：77
- VSIX 大小：819,495 bytes
- SHA-256：`ff761e018b44f1aa51e6f9c9d374847f72e503ada27d2e67aacfa2d7193c989e`
- 源码 `package.json`、`package-lock.json` 与 lock 根包版本仍为 `2.1.1`；打包通过
  `vsce package 2.3.0 --no-update-package-json` 注入测试包版本，没有污染源码版本。
- 当前仅生成本地包；未自动安装、未 push、未创建 PR、未发布 Release。

## V2.3.0 实际范围

### 1. 三页 Codex 体验

- **概览**：最近任务、最近 7 天、最近 30 天、全部时间；展示 processed、fresh、
  cached input、output、reasoning、趋势、构成、覆盖率、质量和最后观测额度。
- **探索**：项目、会话、模型与推理强度；支持搜索、筛选、排序、图表日期下钻、
  项目下钻、父子谱系折叠与状态恢复。
- **优化建议**：只针对用户选定范围，根据真实聚合证据显示“观测—证据—代理指标—
  条件式行动”；没有证据时不生成泛化建议。
- **设置**：供应商感知的辅助页面，可分别关闭 Codex 数据收集和本地建议；返回时保留
  原主页面。Codex watcher 默认安静等待 30 秒，也可关闭或设为 10／60／120／300 秒。
- 桌面、移动、亮色、暗色均沿用插件原有卡片、柱状图、构成图、表格、间距和
  VS Code theme token，避免形成“插件里嵌了另一个插件”的视觉割裂。

### 2. 真实 session 与项目名称

- 精确流式读取 `$CODEX_HOME/session_index.jsonl` 的 `id → thread_name` 关系；只把
  清洗后的标题保留在内存，不写入持久索引。
- root 使用最新真实标题；child 优先使用自己的真实标题。child 没有标题时，才使用
  nickname + parent／root 标题；仍无法确定时使用本地化中性名称。
- 最近任务的项目显示只从同一规范化项目的记录中选取；匿名 root 不会再阻止同项目
  child 提供真实项目名，也不会误借其他项目的名称。task key、root 标题和父子谱系保持不变。
- 标题中的绝对路径会先替换为 `[path]`。项目优先使用 Git 仓库名，否则只使用工作
  目录 basename；不会把完整路径、URL 或原始 session ID 暴露到 UI 或索引。

### 3. 数据与统计语义

- 用量记录只发现 `sessions/**/*.jsonl` 与 `archived_sessions/**/*.jsonl`；
  `session_index.jsonl` 仅用于标题关联。credential、数据库、浏览器状态、Keychain 和
  未知文件均不纳入发现范围。
- 最近 7／30 天按设置时区中的真实事件日滚动计算；覆盖未完成时明确显示 partial，
  不把部分回填冒充完整统计。
- root、child、approval-reviewer 与无法可靠分类的剩余用量分别显示；剩余量明确标为
  root／unknown-role，不再强行分配成已知角色。
- Codex 限额是日志中的最后观测快照，过期后隐藏；不是在线 billing API。
- Claude 继续保留原成本与配额语义。跨供应商比较不会相加不兼容的 cost 或 quota。

### 4. 隐私与持久索引

- 用量 JSONL 会逐行流式、临时解析，以提取白名单内的用量与结构元数据；prompt、
  response、command、tool argument 字段不会被检查或用于分析，也不会被保留或持久化。
- 持久索引只保存带机器 salt 的伪匿名 key、数字／结构聚合，以及清洗后的项目／目录、
  agent、model、effort、role、时间与质量元数据。
- 持久索引不保存原始 ID、完整路径、URL、thread title 或对话正文。
- agent nickname、model 和 effort 在解析入口与索引 load／save 两端使用同一白名单净化；
  路径、URL、控制字符、UUID 和 64 位伪匿名键都会被丢弃。危险 bucket 归入 `unknown`
  并合并 token，避免因清洗而减少总量。
- worker 支持 append tail、truncate／replace、archive move、失败保留上次可信结果、
  single-flight、取消和安全 checkpoint。
- warm refresh 的“零重读”只指未变化的 usage-record／rollout JSONL；精确标题映射文件
  `session_index.jsonl` 每次仍会流式读取，文档不再把这两类文件混为一谈。
- 损坏 JSON 与不支持的持久索引 schema 会先被原子改名为带时间戳和进程号的
  `.corrupt-*.json`，随后从本地 usage records 重新建立；不会从损坏聚合中拼接或猜测
  数据。诊断只显示 `invalid-json`／`unsupported-schema` 安全枚举，不包含路径和内容。
- 本次实际损坏索引已保留为
  `/Users/carl/Library/Application Support/Code/User/globalStorage/growthjack.claude-code-usage/codex-index-v1.corrupt-manual-20260722.json`
  （1,336,207 bytes），原活动索引位置已腾空供首次重建。该备份未删除。

### 5. 使用行为优化

- 基于所选范围识别高 effort、多 agent、自动审批审阅、重复检查代理指标、修改后检查
  和缓存行为，并给出八种 UI 语言的可粘贴约束。
- 建议由本地确定性规则从白名单聚合生成，不调用模型或网络，也不检查或使用正文字段。
- `apply_patch` 轮次、修改后工具调用等只标为结构性代理指标，不宣称它们等于实际文件
  数或能够证明因果关系。
- 这能直接回应 Codex 5.6 sol 在普通任务上容易进入多轮审阅、独立复审和 subagent
  扩张的问题：先用本地证据指出发生在哪些范围，再建议限制 effort、审阅轮次或 agent
  数量，而不是对所有用户显示同一套空泛提示。

### 6. 发布与维护门禁

- 发布提醒只匹配当前完整版本，默认开启但可关闭；不会继续提醒旧 V2.2 内容。
- 过时的模型专属每周额度选项已移除，保留通用 5 小时／每周窗口。
- GitHub Actions 分为 Node Tests、UI Test、Release Package 三层；发布任务必须等待逻辑
  和浏览器门禁，并在 Marketplace、Open VSX 和 Release 附件之前校验 VSIX。
- Claude 文件 watcher 只刷新 Claude；Codex watcher 独立遵守自己的安静延迟，因此
  Claude 的频繁 JSONL 写入不会绕过 Codex 的 120／300 秒低 CPU 扫描选项。
- VSCE 固定为 3.9.1；包策略拒绝源码、测试、依赖、secret、内部 review 材料、异常路径、
  重复条目、错误 runtime main 和版本不一致。
- Codex 主写 commit 使用 OpenAI Codex footer 与 co-author trailer；历史贡献者 PR 的
  merge 署名保持不变。

## 最终验证证据

| 门禁 | 环境 | 结果 |
|---|---|---:|
| TypeScript + Node／策略测试 | `node:20-bookworm`，Node v20.20.2 | 497 项：496 pass、0 fail、1 skip |
| Webview 交互 | Playwright Chromium Linux | 19/19 |
| 无障碍 | Playwright Chromium Linux + Axe | 12/12 |
| 8 locale 响应式 | 360／480／1280 px | 19/19 |
| 视觉回归 | 亮／暗、桌面／移动 | 6/6 |
| Webview 合计 | 固定 `mcr.microsoft.com/playwright:v1.61.1-noble` | 56/56 |
| VSIX 规则校验 | 仓库内 `verify-vsix.mjs` | 通过，77 files／819,495 bytes |
| ZIP 完整性 | `unzip -t` | 全部条目 OK |

唯一跳过项是 inode 替换 fixture：当前容器文件系统不能为该 fixture 精确保留同一
mtime；测试明确以这一能力为条件，未发生失败。

六张 Linux Chromium 视觉基线：

| 场景 | 尺寸 | SHA-256 |
|---|---:|---|
| Explore desktop light | 1248×465 | `b66fcb525528a6911b341d5222b286ad82936b6e6e261fa903a558008ec3608e` |
| Overview desktop dark | 1248×2422 | `3cd1276eb54165d43b5e95c3184cda11c98714f7794bf7c78fcc11571d44717f` |
| Overview desktop light | 1248×2422 | `ee00628dc0152ecbc2924aaccb053df4c820db0c4e5d32edace342132b89f355` |
| Overview mobile light | 328×3258 | `d84e116c7098d31083105537e9ec50745e99c717ad2809b6afbdde48bff60b9b` |
| Recommendations desktop light | 1248×792 | `1b8888062aaa4bb22b3ef6400febe74161a57cd329ddd3ca2985365ce0045b8d` |
| Sessions mobile light | 328×1734 | `ce7d24a7e5421f63cf291c52b1a386ad45b42ce29220cb43be7840913bdb090e` |

包内没有 `.superpowers`、`.github`、`src`、`out/test`、`tests`、`node_modules`、
`docs`、agent 状态、credential、secret 或私钥材料。

## 已完成与尚未完成的运行环境验证

- 之前的 macOS 隔离 RC 已在全新 VS Code user-data／extensions 目录安装并激活成功；
  合成 Codex-only 数据也成功索引。
- R5 没有自动安装，留给维护者在日常 VS Code 环境中验收实际视觉和真实历史数据。
- 本轮已在 Linux Node 20 完成全部逻辑／策略门禁，并在 Linux Chromium 完成 56 项真实
  Webview 测试。
- 还没有在真实 Linux VS Code Extension Host 上做长期 CPU smoke；因此 Linux 高 CPU
  风险已有 worker、single-flight、quiet delay 和自动化保护，但仍建议在 prerelease／
  PR 阶段观察一次实际 Extension Host CPU。

## 已知 Beta 边界

- Codex 不显示美元成本；本地日志不能可靠支持时，不会补造 Branches／Workflows。
- 限额不是在线余额；新写入日志出现前，最后观测值可能滞后。
- 大历史第一次 backfill 仍会消耗磁盘读取，但在可取消 worker 中执行；后续只增量读取
  变化的 usage-record／rollout 文件。
- 产品在 Beta 期间沿用现有 Marketplace identity；是否改名或拆包留待采用率复盘。
- R5 是本地候选包，不等于已发布版本。

## 本地验收方法

1. VS Code 打开 **Extensions**。
2. 点击右上角 `…`，选择 **Install from VSIX…**。
3. 选择项目根目录的
   `claude-code-usage-2.3.0-codex-beta-r5.vsix`。
4. 执行 `Developer: Reload Window`。
5. 重点检查真实 session／项目名称、概览的时段与额度、探索的项目／会话下钻、优化建议
   的证据范围，以及 Settings 返回原页面的状态保持。

## 后续发布路径

当前按维护者要求停在本地候选包；没有替维护者 push、开 PR 或发布。完成本地验收后：

1. push `codex/v2.3.0-codex-beta`；
2. 创建 V2.3.0 PR，等待 Node Tests、UI Test、Release Package 全绿后合并；
3. 检查 Release Drafter 生成的 `v2.3.0` 草稿；
4. 由维护者点击 **Publish release**。

不要手改源码版本，也不要手建 tag。发布 workflow 会从 `v2.3.0` tag 在构建时写入
2.3.0，并使用现有 `VSCE_PAT`、`OVSX_PAT` 发布两个 registry 后附加已验证 VSIX。

## 可直接使用的 PR 正文（英文）

```md
## Summary

- Adds an opt-out, local-only Codex Beta alongside the existing Claude experience, with provider-native processed, fresh, cached-input, output, reasoning, model, effort, coverage, quality, and last-observed limit semantics. Codex dollar cost is intentionally not estimated.
- Rebuilds Codex as three cohesive destinations in the existing dashboard design system: Overview, Explore, and Recommendations. Settings remains a provider-aware auxiliary page that returns to the previous destination.
- Adds truthful project and thread identity, searchable and sortable project/session drill-downs, parent/child lineage, exact event-day rolling periods, partial-coverage disclosure, and a truthful root/unknown-role residual instead of inventing classifications.
- Adds selected-scope, evidence-gated local guidance for effort, multi-agent work, automatic approval reviews, repeated-check proxies, post-patch checks, and cache behavior without inspecting or using conversation fields, which are never retained.
- Indexes large Codex histories in a cancellable background worker with pseudonymous persistent aggregates, append-tail refreshes, safe recovery, and a quiet configurable watcher.
- Adds Node 20, real Chromium Webview, visual-regression, accessibility, responsive, and verified-VSIX release gates.

## How was this tested?

- Node 20: 497 tests, 496 passed, 0 failed, 1 filesystem-capability fixture skipped.
- Playwright Chromium Linux: 56/56 passed (19 interactions, 12 accessibility, 19 responsive/localization, 6 visual baselines).
- The R5 VSIX passed the repository package verifier and ZIP integrity check: 77 files, 819,495 bytes, SHA-256 `ff761e018b44f1aa51e6f9c9d374847f72e503ada27d2e67aacfa2d7193c989e`.
- A previous isolated macOS RC installed and activated successfully. A real Linux VS Code Extension Host CPU smoke remains recommended during prerelease review.

## Checklist

- [x] Claude-only behavior remains available
- [x] Codex-only and both-provider selection are covered
- [x] No fabricated Codex dollar cost, Branches, or Workflows
- [x] Real thread/project identity and privacy boundaries are covered
- [x] All eight UI locales and seven README editions are synchronized
- [x] Node, browser, accessibility, responsive, visual, and package gates pass
- [x] `CHANGELOG.md` updated
- [x] Codex-led commits use the OpenAI Codex co-author trailer
- [ ] Real Linux Extension Host CPU smoke (recommended during prerelease review)

---
🤖 Generated with [OpenAI Codex](https://developers.openai.com/codex/)
```

## 可直接使用的 Release notes（英文）

```md
## V2.3.0 — Codex Beta and evidence-based usage coaching

### Added

- **A cohesive three-page Codex dashboard**: Overview summarizes recent, 7-day, 30-day, and all-time usage; Explore covers projects, sessions, models, and effort; Recommendations turns selected-scope evidence into conditional actions. All three use the existing Claude dashboard's cards, charts, tables, spacing, and VS Code theme tokens.
- **Truthful local identity**: real Codex thread titles are streamed from the local session index, child threads prefer their own titles, and projects use Git repository names or safe folder basenames. Titles are path-redacted and never persisted.
- **Provider-native usage semantics**: processed, fresh, cached input, output, reasoning, model, effort, lineage, coverage, quality, and last-observed limits are shown without estimating Codex dollar cost or inventing Branches and Workflows.
- **Evidence-gated local recommendations**: selected scopes can surface high effort, multi-agent work, automatic approval reviews, repeated-check proxies, post-patch checks, and cache patterns. Guidance runs locally without a model or network call, does not inspect or use conversation fields, and never retains them.
- **Scalable private indexing**: a cancellable worker stores only pseudonymous keys, safe metadata, and numeric/structural aggregates, then refreshes changed rollout tails instead of rereading unchanged usage files.
- **Release quality gates**: Node 20 tests, real Chromium interactions, accessibility, eight-locale responsive coverage, six visual baselines, and strict VSIX verification now gate publishing.

### Changed

- Release announcements now match only the exact installed version and remain default-on but disableable.
- Codex watching defaults to a quiet 30-second delay and can be disabled or extended to 300 seconds.
- The obsolete model-specific weekly-limit option is retired; generic 5-hour and weekly windows remain.

### Privacy and boundaries

- Usage discovery is restricted to local session and archived-session rollout JSONL. A separate exact session index is streamed only for title mapping.
- Credentials, databases, browser state, Keychain state, unknown files, raw IDs, full paths, URLs, titles, prompts, responses, commands, and tool arguments are excluded from persistent storage.
- Limits are last-observed local-log snapshots, not live billing data. Codex cost is not estimated.

---
🤖 Generated with [OpenAI Codex](https://developers.openai.com/codex/)
```

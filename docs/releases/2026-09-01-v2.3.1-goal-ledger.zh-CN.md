# Claude Code Usage v2.3.1 Goal 执行台账

本台账记录本地候选从 G0 到 G6 的阶段状态、可复现验收证据、Claude CLI 只读审阅和剩余风险。它不记录账号、凭证、真实日志、提示词、线程标题或本地路径。

## 执行边界

- 分支：`codex/v2.3.1-goal-g0-g6`
- 基线：`7fd03f3183240940d0a2d1cfe76f60cb80f0a6ae`
- 工作方式：隔离 worktree；用户主 checkout 及其中已有修改保持只读。
- 允许：本地代码/文档修改、测试、构建、VSIX、阶段性本地 commit、指定检查点的 Claude CLI 只读审阅。
- 禁止：push、Marketplace 发布、远程 release、真实 GitHub 写入、上传用户日志或身份数据。

## 阶段状态

| 阶段 | 状态 | 本地 commit | 验证摘要 | Claude 审阅 | 剩余风险 |
|---|---|---|---|---|---|
| G0 基线与夹具 | 进行中 | 待记录 | Node 795 passed / 1 skipped；UI 90 passed | 不适用 | 完成统一 fixture 登记并提交 |
| G1 数据契约 | 未开始 | — | — | 待审阅 | — |
| G2 统计/排序/effort | 未开始 | — | — | 不适用 | — |
| G3 额度观测与估算 | 未开始 | — | — | 待审阅 | — |
| G4A 视觉/交互/可访问性 | 未开始 | — | — | 不适用 | — |
| G4B 综合热力图与分享 | 未开始 | — | — | 待审阅 | — |
| G5 README/截图 | 未开始 | — | — | 待审阅 | — |
| G6 发布候选 | 未开始 | — | — | 不适用 | — |

## G0 基线证据

### 环境与测试

- Node：`v24.19.0`
- npm：`11.17.0`
- `npm ci`：成功。
- `npm run test:node`：796 项，795 通过，0 失败，1 跳过。首次受限运行中的两个 loopback `EPERM` 由测试沙箱引起；允许本地测试服务器绑定后均通过。
- `npm run test:ui`：90/90 通过。
- 基线工作树：干净。

### v2.3.1 最小回归夹具登记

| 风险 | 固定输入/测试 | 验收事实 |
|---|---|---|
| 最近 30 天大于全部时间 | `codexUsage.test.ts`：`an inflated period sidecar falls back to the verified daily aggregate`；`v2.3.1 acceptance fixture keeps non-negative ranges monotonic and reconciled` | `today ≤ last30Days ≤ allTime`，日/月汇总与总计可核对 |
| 重复索引/重复累计 | `codexIndex.test.ts`：`fully indexed unchanged corpus returns a warm no-op without body reads`；`cold scan, appended tail, and persisted reload agree` | 连续刷新为稳定 no-op，重载后总计不变 |
| 活跃/归档重复文件 | `codexIndex.test.ts`：`exact active and archive copies contribute usage only once` | 同一内容只计一次并报告去重覆盖 |
| 月度顺序 | `codexUsage.test.ts`：`all-time, monthly, and behavior views stay provider-native` | 图表/表格源数据按月份正序 |
| 连续/不规则额度重置 | `codexQuotaHistory.test.ts`：`irregular reset boundaries survive polling compaction`；`weeklyValue.test.ts` 的 fresh reset/irregular reset fixtures | 独立 reset 被保留，窗口不重复计量 |
| unknown 推理强度 | `codexParser.test.ts` 的 current/legacy/missing/unsafe context fixtures；`codexUsage.test.ts`：`missing model and effort values are grouped as unknown` | 可解析值保留；确实缺失或被安全拒绝时进入 unknown |

### G0 通过条件

- [x] 隔离候选，不触碰用户 checkout。
- [x] 记录 branch、HEAD、状态和基线测试。
- [x] 为五类已知回归建立可重复的自动化夹具索引。
- [x] 加入跨视图非负、单调、日/月核对断言。
- [ ] 提交 G0 台账与新增验收断言。

## 后续更新规则

每阶段必须先完成对应自动化验证，再更新本表中的 commit、测试证据、Claude 结论和风险。G6 的最终验收矩阵应由本台账中的证据生成；任何未通过项不得用截图或文案替代。

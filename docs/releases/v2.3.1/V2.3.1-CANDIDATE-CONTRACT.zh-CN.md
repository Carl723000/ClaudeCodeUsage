# v2.3.1 本地候选：功能与资源契约

配套文档：[中文契约](V2.3.1-CANDIDATE-CONTRACT.zh-CN.md) · [English contract](V2.3.1-CANDIDATE-CONTRACT.md)

## 2026-09-04 发版前核对

- 当前候选在 `5f3db3a` 之上只补齐 Claude 独立审计指出的授权撤回问题：立即作废旧预览、阻止等待持久化期间的新预览/发送，并中止在途建议请求；独立 Optimizer 请求不受影响。Claude 只读审计，修复与回归由 Codex 实施。
- 完整 Node：931 通过、0 失败、1 项既有文件系统能力跳过；完整 Playwright：124/124 通过。新增 6 个行为回归及宿主回调接线检查先失败、修复后通过。
- 本地包：`claude-code-usage-2.3.1-release-ready-20260904.vsix`，版本 2.3.1，107 文件，1,917,142 bytes；SHA-256：`0d1beaedb3bcf28467f776462e6028f4319a6c36944518e25194755067df8b8e`。此包取代此前 overnight 候选，不混入后续版本预备功能。
- 原生 VS Code 1.135.0 隔离安装成功，入口与 Webview 的安装文件哈希均与测试构建一致，观察到扩展激活且无本扩展异常。Mac 锁屏使最后的原生界面点验受阻；浏览器回归不替代这一步，也未做真实 BYOK 网络调用。
- 上游只读核对：main 为 `eca4e43`，最新已发布版本为 v2.3.0，open issue 为 #94 / #91，open PR 为 0。候选的发布/测试 workflow 与当前上游一致；#94 的兼容参数修复已包含，#91 多币种延期。没有推送、PR、合并、tag 或发布操作。
- 发版前仍需维护者解锁后点验原 profile 的 Claude 今日/30 天及 Codex 额度。原环境偶发零值尚未稳定复现；早期无持久会话的 CLI 调用不能回填，新的标准持久会话保留正常日志。Windows/Linux 安装态烟测仍未完成。
- Claude 额度已由维护者报告耗尽，本轮没有再次调用；保留夜间任务到 08:00，额度等待期间只继续本地验证与后续整理。

## 用户增益

v2.3.1 把现有建议收敛成一条可验证的闭环：用户先看到建议所依据的本地聚合证据和成功标准，再决定是否使用可选的 AI 个性化；之后可以记录“有用 / 无用 / 已应用”，并用满足相同 provider、任务类型、复杂度、测量口径和质量护栏的任务对判断是否观察到改善。证据不足时只显示“证据不足”，不把波动写成因果结论。

同版补齐 Codex 最近 30 天的日期到小时展开。任一有数据日期都从主索引已经保存的稀疏小时聚合读取，点击时不读原始 JSONL；Claude 与 Codex 使用同一配置时区和 `HH:00` 标签。

## 本轮最远边界

- AI 发送只复用现有合法 BYOK / 用户配置 endpoint，并且只由用户点击触发；没有默认、后台、刷新触发或隐藏页面触发的 AI 请求。
- host 只准备一次 `PreparedAdvicePayload`。预览、字节数、SHA-256、重试与实际请求体都消费同一对象的同一组 canonical UTF-8 bytes，任何重建或不一致都失败关闭。
- 默认只包含 allowlist 聚合。prompt 个性化是独立、默认关闭、可撤回的同意；只有用户提供或明确选择的文本才能逐字进入预览和发送。
- 反馈、可比任务对和版本化 comparison result envelope 只保存在本地有界状态中；不保存 prompt、response、session、路径、项目名、原始记录或自由文本反馈。
- 旧 Get AI Advice 与 Prompt Optimizer 复用同一准备、预览、发送、解析和反馈边界；Optimizer 仍只处理用户粘贴的草稿，不把宽松文本 fallback 当作结构化建议。
- 不做账户中心、认证、多账户切换、成就页、GitHub、默认或后台 AI 请求；不改版本号、默认设置或 release workflow，也不发布。

## 唯一数据生产者

- Claude 原始日志的唯一读取者是 `ClaudeUsageIndex`；Codex 原始日志的唯一读取者是现有 persistent provider index 与其 worker。
- advice、feedback comparison、小时展开和 Webview 只消费带 `provider + measurementProfileVersion + revision + coverage` 的主索引聚合或其有界投影；它们不建立第二套 scanner、缓存真相或 scheduler。
- Codex 最近 30 天稀疏 `date -> hour` 桶由同一次主索引增量处理维护，并在配置时区下淘汰第 31 天。一次补齐迁移由现有 Codex worker 执行并复用同一持久进度状态。

## 资源所有权与停止条件

| 资源 | 创建者 | 创建条件 | 停止条件与实际销毁者 |
| --- | --- | --- | --- |
| Claude / Codex 周期 timer 与 watcher | `extension.ts` 的既有窗口生命周期协调器 | provider 启用且窗口处于允许状态 | 失焦、provider 关闭或 extension dispose；由生命周期协调器清理 |
| Codex index / backfill worker | 现有 `CodexIndexClient` | 主索引需要 cold build、兼容迁移或增量工作，且 E-03 允许执行 | 完成、预算耗尽并 checkpoint、用户暂停、provider 关闭或 extension dispose；由 client 终止 worker。首次可用性 backfill 可在失焦后有界继续，但不能越过明确暂停、关闭或 dispose |
| AI 网络请求 | 唯一 host sender | 用户预览后主动点击发送，Prepared 对象仍有效，且 BYOK 配置合法 | 响应、错误、用户取消、同意撤回、功能关闭或 extension dispose；由 sender 的 cancellation owner 中止。不存在 retry timer 或后台轮询 |
| 建议投影与 comparison | 不创建长期资源 | 仅在主索引 revision 变化、用户反馈或相关页面按需展示时执行纯投影 | 同步完成即释放；页面隐藏或功能关闭后不重算、不重绘 |
| E-03 进度记录 | 主索引 durable state | migration / backfill 第一次需要时 | 不是运行资源；完成后只保留版本、完成位置、`failStreak`、`nextEligibleAt`、`pausedReason` 与必要统计 |

E-03 的成功路径立即继续，不人为等待。只有失败、无进展或明确暂停才设置持久 cooldown；普通 refresh、watch、focus、reset、菜单打开或插件升级不能绕过 `nextEligibleAt`。重启从已验证 checkpoint 继续。

## 关闭后的零新增长期资源

相对 v2.3.0：建议闭环关闭、未同意、未发送或页面隐藏时，新增资源必须为 0 timer、0 watcher、0 worker、0 network、0 额外日志枚举 / 正文读取、0 隐藏 Webview 重绘。Codex 基础索引迁移结束或停止后资源清单必须回到基线；不存在为“以后可能有用”保留的空循环。

## 验收边界

- red-green 聚焦测试先覆盖 canonical bytes、显式发送、严格解析、真实 pair ingestion、版本化 comparison envelope、资源登记 / 销毁、持久 backoff / resume 和 30 天小时展开。
- warm unchanged 为 0 JSONL body read、0 派生 mutation、0 durable write；展开日期为 0 JSONL read，小时合计与当日汇总一致。
- 所有用户文案覆盖八个 locale；UI 状态具备“操作 -> full reload -> 状态保留 / 快照失效”测试。
- 最终运行 compile、聚焦与全量 Node/UI 测试、关闭 / 未授权零资源、warm unchanged、VSIX 清单及版本 / 设置 / workflow 边界检查；可行时完成 F5 与 installed-VSIX smoke。

---
🤖 Generated with [OpenAI Codex](https://developers.openai.com/codex/)

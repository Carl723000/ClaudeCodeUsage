# Claude Code Usage v2.3.1 Goal Ledger

This tracked ledger records G0–G6 stage status, reproducible acceptance evidence, read-only Claude CLI reviews, and residual risk. It never stores accounts, credentials, real logs, prompts, thread titles, or local paths.

## Execution boundary

- Branch: `codex/v2.3.1-goal-g0-g6`
- Baseline: `7fd03f3183240940d0a2d1cfe76f60cb80f0a6ae`
- Workspace: isolated worktree; the user's primary checkout and pre-existing edits remain read-only.
- Allowed: local code/document changes, tests, builds, VSIX packaging, stage commits, and the specified read-only Claude CLI checkpoints.
- Forbidden: push, Marketplace publication, remote releases, real GitHub writes, or uploading user logs and identity data.

## Stage status

| Stage | Status | Local commit | Verification | Claude review | Residual risk |
|---|---|---|---|---|---|
| G0 baseline and fixtures | Complete | `f0af506` | Node 795 passed / 1 skipped; UI 90 passed; targeted 29 passed | N/A | No blocking risk |
| G1 data contract | Complete | `86d95df` | Focused 17 passed; Node 806 passed / 1 skipped; compile passed | Conditional GO, then revision GO | Blocked legacy workspace plaintext remains until explicit recovery; host SecretStorage backing varies; unified quota store is a G3 deliverable |
| G2 statistics/order/effort | Complete | `49f2041` | Node 815 passed / 1 skipped; UI 90 passed; all focused regressions passed | N/A | Unified observation/account attribution remains a G3 concern |
| G3 quota observations/estimates | Complete | `a04ab04` | Node 841 passed / 1 skipped; UI 90 passed; compile passed | GO, then revision GO | Quarantined corrupt/future files remain on disk until the data-clear path or a future cleanup policy removes them |
| G4A visual/interaction/accessibility | Complete | `fca21da` | Node 841 passed / 1 skipped; UI 107 passed; focused G4A 68 passed | N/A | Provider-native metric semantics and card counts intentionally remain different |
| G4B combined heatmap/share | Complete | `1e6bbab`, `5e5bf9f` | Node 861 passed / 1 skipped; UI 112 passed; focused 69 + 5 passed | GO, then revision GO | GitHub `public_repo` remains account-wide for public repositories; local SVG/Markdown requires no scope and is the default safe path |
| G5 README/screenshots | Not started | — | — | Pending | — |
| G6 release candidate | Not started | — | — | N/A | — |

## G0 baseline evidence

### Environment and tests

- Node: `v24.19.0`
- npm: `11.17.0`
- `npm ci`: successful.
- `npm run test:node`: 796 tests, 795 passed, 0 failed, 1 skipped. Two loopback `EPERM` failures in the first restricted run were test-sandbox artifacts; both passed when the local test servers were allowed to bind.
- `npm run test:ui`: 90/90 passed.
- Baseline worktree: clean.

### v2.3.1 minimum regression fixture registry

| Risk | Fixed input/test | Acceptance fact |
|---|---|---|
| Last 30 days exceeds all time | `codexUsage.test.ts`: `an inflated period sidecar falls back to the verified daily aggregate`; `v2.3.1 acceptance fixture keeps non-negative ranges monotonic and reconciled` | `today ≤ last30Days ≤ allTime`; daily/monthly totals reconcile |
| Reindex double counting | `codexIndex.test.ts`: `fully indexed unchanged corpus returns a warm no-op without body reads`; `cold scan, appended tail, and persisted reload agree` | Repeated refresh is a stable no-op and reload preserves totals |
| Active/archive duplication | `codexIndex.test.ts`: `exact active and archive copies contribute usage only once` | Identical content contributes once and dedup coverage is reported |
| Month ordering | `codexUsage.test.ts`: `all-time, monthly, and behavior views stay provider-native` | Chart/table source rows are chronological |
| Consecutive/irregular quota resets | `codexQuotaHistory.test.ts`: `irregular reset boundaries survive polling compaction`; fresh/irregular reset fixtures in `weeklyValue.test.ts` | Distinct resets remain distinct and windows are not double-counted |
| Unknown reasoning effort | Current, legacy, missing, and unsafe context fixtures in `codexParser.test.ts`; `missing model and effort values are grouped as unknown` in `codexUsage.test.ts` | Parseable values survive; only missing or safely rejected values become unknown |

### G0 exit gate

- [x] Isolate the candidate and leave the user checkout untouched.
- [x] Record branch, HEAD, status, and baseline tests.
- [x] Register automated fixtures for every known regression family.
- [x] Add a cross-view non-negative, monotonic, daily/monthly reconciliation assertion.
- [x] Commit the G0 ledger and acceptance assertion (`f0af506`).

## G1 local-data and privacy evidence

### Contract and implementation

- Added the bilingual normative local-data contract covering raw sources, indexes, derived statistics, quota observations, reset events, UI/share preferences, schema migration, retention, clearing, and every remote boundary.
- Defined anonymous account fingerprints, atomic/deduplicated observation writes, per-account isolation, and the prohibition on credentials, complete identifiers, raw CLI output, and private source content.
- Moved the advice BYOK key from contributed configuration/globalState into VS Code SecretStorage. Webview snapshots expose only `configured`, and the password field never rehydrates a stored secret.
- Migration inspects Global, current Workspace, and every open Workspace Folder. It automatically migrates only one unambiguous machine-wide value, writes secure storage first, clears plaintext second, and fails closed with localized recovery copy on workspace-only, conflicting, or provider-failure cases.

### Verification and review

- Focused privacy/contract suite: 17 passed.
- Full Node suite on an isolated loopback port: 807 tests, 806 passed, 0 failed, 1 filesystem-capability skip.
- Claude CLI first review: conditional GO, requesting complete scope enumeration, concurrency/idempotency protection, fixed failure UX, and explicit Settings Sync evidence.
- Revision review: GO to G2 with no blocker. Non-blocking caveats about blocked workspace plaintext and host-dependent SecretStorage backing are now explicit in the contract.

### G1 exit gate

- [x] Track a bilingual normative data contract in the repository.
- [x] Make every persisted data class, retention rule, clear path, and remote interaction auditable.
- [x] Migrate the only credential to SecretStorage without exposing it to Webview state.
- [x] Fail closed and preserve recoverability for ambiguous or failed legacy migration.
- [x] Pass the G1 test gate and Claude revision review.

## G2 entry audit

The following implementation risks are test hypotheses, not accepted behavior. Each must first be reproduced by a minimal fixture before its implementation changes:

1. Weekly pricing coverage appears to use the whole window rather than the observation-time prefix.
2. Exact-content deduplication does not include day/period distribution and can collapse records with equal totals but different dates.
3. A `codex-spark` limit may be classified account-wide when its scope is model-specific.
4. A delayed post-reset current observation may mask a valid cached pre-reset observation.
5. Cumulative-delta repair may permit `reasoning > output` or `cached input > input`.
6. Residual usage in a partial effort map may disappear instead of being attributed to `unknown`.

Additional guards cover the complete 30-day definition in the configured timezone, cross-year chronological month order, alias/invalid reasoning-effort variants, and conservation across daily, monthly, model, effort, and total views.

## G2 correctness evidence

### Root-cause fixes

- Exact active/archive deduplication now includes the authoritative all-time `byDay` distribution, so equal totals on different dates cannot silently collapse. Rebuilding `period`/`today` projections intentionally remain outside identity, preserving refresh idempotence while their independent readability checks run.
- Weekly allowance inference gates on priced-token coverage up to the observation timestamp, not later rows in the same week. A later priced row can no longer make a weak prefix look sufficiently priced.
- An explicit model-specific Codex `limitId` takes precedence over a generic `limitName`, preventing Spark and similar allowances from entering the account-wide series.
- A delayed poll written at or after reset no longer shadows a useful cached pre-reset observation.
- Cumulative token deltas clamp cached input to input and reasoning to output, mark the repaired event partial, and continue to count processed tokens exactly once.
- Partial model/effort maps send positive residual components to `unknown`; zero-value buckets are removed from aggregate and thread projections.

### Actual-schema effort audit

- A local field-path-only audit emitted counts, never filenames, values, timestamps, or log text. It observed 51,912 `turn_context.payload.effort` fields, 33,403 `thread_settings_applied` variants, and 33 explicit completed-item variants during the bounded scan.
- Every string encountered by the allowlisted paths normalized to `none`, `minimal`, `low`, `medium`, `high`, `xhigh`, `max`, or `ultra`; one explicit null remained unattributed. The parser now accepts these explicit current/legacy paths and never infers effort from model names.

### Verification

- Focused red/green fixtures cover authoritative day distribution, sidecar migration stability, observation-prefix pricing coverage, explicit limit-ID precedence, delayed rollover polls, cumulative component repair, real effort schema variants, partial/zero unknown buckets, 30-day conservation, and cross-year month order.
- Final full Node suite: 816 tests, 815 passed, 0 failed, 1 filesystem-capability skip.
- Final Playwright suite: 90/90 passed, including accessibility, keyboard, light/dark, 360 px, chronology, and Compare regressions.

### G2 exit gate

- [x] “Last 30 days” remains today plus the previous 29 configured-zone civil dates and reconciles with its daily/model/effort rows.
- [x] `today ≤ last 30 days ≤ all time`; daily/monthly totals reconcile with all time.
- [x] Exact duplicates are stable across refresh while differing day distributions are retained as ambiguous.
- [x] Monthly chart and table data are chronological, including a year boundary.
- [x] Explicit reasoning-effort variants normalize; missing/invalid evidence becomes `unknown`; zero unknown is hidden.
- [x] Full Node and browser gates pass.

## G3 quota-observation and weekly-estimate evidence

### Persisted observation boundary

- Added a schema-versioned, provider/account/window-partitioned observation store with machine-local HMAC fingerprints. Raw credentials and identity signals are discarded after fingerprinting; Webview projections receive only per-run ephemeral aliases.
- Observation writes use a same-directory `0600` temporary file, `fsync`, and atomic rename. A cross-process lease, locked re-read, canonical deduplication, 180-day retention, and bounded compaction make repeated refreshes stable without merging account epochs.
- Strict validation quarantines corrupt or future-schema files instead of trusting or deleting them. Legacy P3/P4 global-state observations and P5 Codex index history migrate P2-first; source keys and migration markers advance only after the destination append succeeds, so an interrupted migration retries safely.
- Claude account continuity is derived only after a successful quota response from a one-way refresh-token HMAC. The token itself is never returned, persisted, logged, or exposed to the Webview. Codex observations use the same store and privacy projection.

### Reset detection and allowance inference

- Reset events are derived at runtime from provider window identity, changed `resetAt`, or a significant used-percentage rollback. Account changes alone do not invent an official reset; same-day and consecutive-day resets remain distinct.
- Every valid observation with `0 < usedFraction ≤ 1` contributes `priced usage through observedAt ÷ usedFraction` as a candidate total. A weighted robust median combines candidates while pricing coverage, log coverage, boundary precision, and attribution determine confidence.
- Estimated total is never lower than confirmed used value. Unused value is clamped non-negative and appears only for a closed window with reliable account attribution, boundary continuity, and sufficient pricing coverage.
- A single coherent unattributed Codex window may expose a low-confidence total but never unused value. Overlapping or multi-account ambiguity degrades to used-only instead of inventing an account split.

### Verification and read-only review

- Focused fixtures cover current/legacy adapters, atomic/deduplicated persistence, concurrent append, deterministic out-of-order identities, corrupt/future quarantine, retention, interrupted migration, token rotation, stale in-flight responses, same-day and consecutive resets, rollback noise, reset changes, duplicate observations, multi-account ambiguity, low pricing coverage, current windows, and non-negative floors.
- Final full Node suite: 842 tests, 841 passed, 0 failed, 1 filesystem-capability skip. Final Playwright suite: 90/90 passed. TypeScript compilation passed.
- Claude CLI checkpoint review: GO with no blocker. It requested legacy-trend hardening, a real migration-write-failure fixture, and explicit account-transition semantics; all were added. The single permitted revision review returned GO for G4A with no blocker, followed by a tighter exact-error assertion.

### G3 exit gate

- [x] Persist immutable, anonymous, per-account quota observations without credentials or stable identifiers in UI state.
- [x] Preserve irregular, consecutive, and same-day reset evidence without treating noise or account switches as official resets.
- [x] Produce a total allowance estimate whenever a valid same-window utilization observation exists, with robust aggregation and explicit confidence.
- [x] Suppress unsafe unused estimates and prevent negative or below-used values.
- [x] Make migration, repeated refresh, and concurrent append idempotent and recoverable.
- [x] Pass full Node/browser/compile gates and both allowed Claude review passes.

## G4A visual, interaction, and accessibility evidence

### One shared visual contract

- Added shared spacing, radius, surface, border, data-font, and focus tokens to the single production stylesheet used byte-for-byte by Claude and Codex. Summary cards, model disclosures, chart controls, empty states, and compact navigation now use that contract without copying provider branding or asserting that unlike metrics are equivalent.
- Kept provider-native labels, card counts, and accounting definitions. Tests compare component geometry and interaction treatment, not provider values or semantic parity.
- At 360 px, provider and dashboard navigation remain one-line horizontal strips. Every Claude/Codex destination plus Compare is checked in all eight UI locales against absolute page overflow, while tables and dense charts retain their own explicit scrollers.

### Keyboard and assistive-technology contract

- Dashboard destinations now implement `tablist` / `tab` / `tabpanel`, roving tabindex, automatic activation, Arrow/Home/End navigation, `aria-selected`, `aria-controls`, and synchronized hidden panels.
- Sortable headers expose keyboard sorting and `aria-sort`. Chart metric controls expose `aria-pressed`; drill-down bars have accessible names, Enter/Space activation, `aria-controls`, and synchronized `aria-expanded`. Claude and Codex model disclosures retain native keyboard behavior.
- Shared `:focus-visible` treatment and reduced-motion handling cover buttons, summaries, inputs, scrollers, sorting controls, and chart drill-downs. Dark-theme Axe coverage found low-contrast description text on the new raised surface; the final design uses the editor canvas plus a border, restoring readable contrast in both Light+ and Dark+.

### Verification

- Focused G4A browser gate: 68/68 passed (24 accessibility/keyboard, 15 responsive, 29 visual/component-contract checks).
- Full Node gate: 842 tests, 841 passed, 0 failed, 1 filesystem-capability skip. Full browser gate: 107/107 passed.
- Privacy-safe fixture renders were inspected for Claude light, Codex light, and Codex dark at 360 px. They confirmed aligned density, chronological reading flow, intact provider semantics, readable dark contrast, and one-line narrow navigation. These inspection images are temporary evidence, not release screenshots; G5 will capture the installed VSIX.

### G4A exit gate

- [x] Reuse one tokenized stylesheet and shared component treatment without creating a second provider design system.
- [x] Preserve honest provider-specific metrics and destinations.
- [x] Pass keyboard, ARIA, focus, reduced-motion, light/dark, eight-locale, and 360 px gates.
- [x] Pass full Node and browser regression suites and visually inspect representative fixture states.

## G4B combined heatmap and sharing evidence

### One aggregate pipeline and deterministic artifact

- Added a pure, configured-timezone daily merge over the existing Claude `DayUsage` map and Codex daily view. Claude processed activity is input + cache creation + cache read + output; Codex processed activity is input total + output total, with cached input already inside input and reasoning already inside output.
- Identical duplicate provider/day rows are idempotent; conflicting duplicates fail closed. Missing dates and single-provider days remain valid, and the Codex daily projection now retains 370 rows so a complete leap-year share window plus boundary margin never triggers a second log scan or cache.
- The combined share card is deterministic for an explicit end date, uses a neutral non-provider palette, exposes Claude/Codex/combined values in every daily tooltip, and accepts only aggregate date/value DTOs. Privacy-canary tests prove that account, project, thread, path, prompt, and log fields cannot enter the SVG.

### Compare workflow and publication boundary

- Compare now leads with the combined heatmap whenever provider activity is available. The user can select 30 days, 90 days, or one year, set an 80-character title, inspect the explicit inclusion/exclusion preview, export SVG, copy a relative Markdown snippet, or reset only the sharing preferences.
- Local export and Markdown copy never request network access. The existing GitHub publisher now requests `public_repo` instead of broad private-repository access, probes repository visibility/default branch and create/update state, fails closed for private or unverifiable targets, and requires a second exact owner/repo/branch/path/action/byte confirmation before the write.
- Destination strings persist only after a successful confirmed write; credentials remain in VS Code authentication. Goal verification uses a mock transport only and performs no real GitHub write.

### Verification and read-only review

- Focused pure/host suites: 69/69 passed, including formulas, duplicate and fully missing dates, symmetric Claude-only/Codex-only input, non-Sunday 30/90-day grid starts, deterministic SVG, privacy canaries, public-only GitHub permission and exact confirmation, sharing reset, and the 370-day source cap.
- Focused browser suite: 5/5 passed for default Compare placement, both-provider tooltips, explicit actions, preference reset, Light+/Dark+ Axe, and 360 px containment.
- Final full Node gate: 862 tests, 861 passed, 0 failed, 1 filesystem-capability skip. Full Playwright gate: 112/112 passed.
- Claude CLI checkpoint returned GO to G5 with no blocker. It identified three low-risk test gaps: symmetric Codex-only input, a date missing from both providers, and 30/90-day SVG starts that are not Sunday-aligned. Commit `5e5bf9f` added direct assertions for all three; the single permitted revision review confirmed each gap closed and returned final GO to G5.

### G4B exit gate

- [x] Reuse existing provider daily aggregates without another source scan or statistics cache.
- [x] Define and test non-duplicating provider formulas, configured-zone ranges, duplicate semantics, and a complete annual source window.
- [x] Deliver deterministic SVG, combined share card, relative Markdown, custom title/range, and a privacy preview in Compare.
- [x] Keep local export offline and harden optional direct GitHub publication with public-only permission, exact confirmation, and mock-only tests.
- [x] Pass focused and complete Node/browser gates.
- [x] Receive the required Claude CLI checkpoint verdict and close every review finding selected for revision.

## Update rule

Each stage must finish its automated verification before this ledger records its commit, evidence, Claude decision, and risk. The G6 acceptance matrix must be derived from these records; screenshots or copy cannot substitute for a failed gate.

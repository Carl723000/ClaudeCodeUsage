# Claude Code Usage v2.3.2 夜间审阅记录

日期：2026-09-03（Asia/Shanghai）

范围：仅在隔离 worktree `codex/v2.3.1-goal-g0-g6` 内审阅、修复与验证；不推送、不合并、不发布，也不操作远程 issue/PR。

## 本轮结论

### 已复现并修复：Codex 线程标题的路径脱敏边界

Claude CLI 对 `src/providers/codex/codexIdentity.ts` 的标题清洗链路做了定向隐私审计，确认 POSIX 绝对路径在紧贴 ASCII 冒号或部分标点时可能绕过原有正则。例如：

- `3:/Users/carl`
- `3:/etc/passwd`
- `:/etc/passwd`

这些标题只在内存中使用，不会持久化或联网，但可能进入仪表盘和截图，因而仍违反“绝对路径不得展示”的隐私边界。

处理方式：先加入上述三类回归样例，编译后的定向测试按预期失败；随后仅放宽路径前缀识别，不改变标题的其他清洗规则。修复后定向测试 15/15 通过。英文与中文本地数据契约同步补充了“路径紧贴标点也必须脱敏”的要求。

### 已审计、无需修改：Codex 周额度状态栏语义

Claude CLI 复核了状态栏、tooltip 和告警阈值的数据流：

- 紧凑状态栏 `wk 64%` 表示“剩余 64%”，符合本轮产品要求。
- tooltip 的进度条和标签显示“已用百分比”，与 Claude 的明细画风一致。
- 告警颜色按已用百分比判断，避免把“剩余越多”错误视为风险。

这是有测试和变更记录覆盖的刻意语义分工，不是本轮缺陷，因此没有为了表面一致而反转数据。

### 已复现并修复：Claude 图表下钻在 Webview 重载后丢失

Claude CLI 对 Claude/Codex 共用图表交互做定向审计，确认 Claude 的两条既有下钻路径没有进入 Webview 状态：

- “最近 30 天”日柱展开到小时后，刷新/重载会收起。
- “全部时间”月柱展开到每日后，刷新/重载会收起。
- 相同场景下，Codex 的已物化日→小时路径已经能够恢复，形成 provider 间的不一致。

先加入两个真实 Playwright 操作测试，均在完整 `page.reload()` 后检查行可见性、选中态、`aria-expanded` 和延迟详情数据请求。修复前两个测试按预期失败；修复后：

- Claude 日→小时、月→日展开状态写入统一 Webview state，并在重载时恢复。
- 恢复过程会重新请求 Claude 的延迟详情数据，但不自动滚动，之后仍由既有滚动恢复逻辑定位。
- 用户主动切换 tab 时仍按既有规则清空展开状态。
- 控件类型改由控件自身所在 tab 判定，不再依赖初始化瞬间的全局活动 tab；这同时修正了“全部时间”非活动 tab 柱状图初次初始化缺少 ARIA 绑定的边界。

第二次 Claude CLI 只读复核认为当前 diff 没有受支持的阻塞缺陷，并确认 Codex 路径未被改变。它指出的非阻塞测试缺口——恢复后是否重新请求 Claude 的 `getHourlyData` / `getDailyData`——也已加入断言。

## Claude CLI 用量与会话

所有调用均使用标准持久会话，未使用 `--no-session-persistence` 或 `--bare`，因此会进入 Claude Code 本地用量日志。

- 基线扫描：114 条 usage 事件；input 228、cache creation 345,157、cache read 11,162,603、output 88,236。
- 路径脱敏审计：47 条 usage 事件；input 94、cache creation 146,606、cache read 2,247,048、output 72,535。
- 周额度语义审计：53 条 usage 事件；input 106、cache creation 271,767、cache read 3,145,242、output 53,757。
- 图表下钻审计（同一持久会话续跑完成）：57 条 usage 事件；input 112、cache creation 144,163、cache read 3,498,499、output 34,409。
- 下钻修复独立复核：67 条 usage 事件；input 134、cache creation 176,071、cache read 5,169,897、output 47,671。
- 截至本轮合计：338 条 usage 事件；input 674、cache creation 1,083,764、cache read 25,223,289、output 296,608（均为 Claude Code 本地日志口径，未触发 rate limit）。

## 验证记录

- 红灯：`node --test out/test/codexIdentity.test.js` 在 `3:/Users/carl` 样例上失败，证明测试能捕获问题。
- 绿灯：修复后同一套定向测试 15/15 通过。
- 完整 Node 测试：在允许测试进程监听本机 loopback 后，906 通过、0 失败、1 跳过。受限 sandbox 内的首次运行仅有两个 `listen EPERM`，均由测试夹具无法绑定 `127.0.0.1` 引起；解除该限制后相同测试通过。
- `git diff --check` 通过；TypeScript 编译通过。
- 路径脱敏本地提交标题：`fix: harden Codex title path redaction`。
- Claude 下钻红灯：新增两个 Playwright 测试后，日→小时未写入状态，月→日缺少初始化 ARIA，两个测试均失败。
- Claude 下钻绿灯：定向测试 2/2 通过；完整交互文件 16/16 通过；既有 Codex 键盘下钻可访问性测试 1/1 通过。
- Claude 下钻修复后的完整 Node 回归：906 通过、0 失败、1 跳过。
- Claude 下钻修复后的完整 Playwright UI 回归：121/121 通过。
- 隔离的 `CCU Release VSCode` 合成数据宿主烟测：Claude“所有”页点击 2026 年 8 月柱后出现每日明细；点击页头刷新后，辅助功能树保留展开明细、选中 provider/tab 与焦点语义，和自动化重载验证一致。
- `git diff --check` 在本轮最终 diff 上再次通过。
- 图表下钻本地提交标题：`fix: persist Claude chart drilldowns`。

## 后续版本建议（尚未实施）

- 用英文界面和浅色主题再走一遍 Today / Last 30 days / All time、柱状图展开和空状态，重点检查中英文文本长度造成的布局差异。
- 在 Windows 环境补充盘符、UNC 与混合分隔符的可视化端到端验证；现有单元测试只证明清洗函数本身的预期行为。
- 继续逐项对照 Claude 与 Codex 的交互细节，但每项先确认 provider 语义，再决定是视觉统一还是保留差异。

## 边界说明

本记录中的“已修复”仅指已由回归测试复现并在本地代码中修正的路径脱敏与 Claude 图表下钻持久化问题；“无需修改”仅指本轮追踪到的周额度状态栏链路。其他初步审阅意见在独立复现前不作为发布结论。
